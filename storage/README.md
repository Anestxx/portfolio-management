# Storage

The app creates its local SQLite database here by default.

- `portfolio_manager.db`: main database file
- `portfolio_manager.db-wal` and `portfolio_manager.db-shm`: SQLite write-ahead logging sidecar files

These runtime files are ignored by git.
